profile, perf command
set up meta progrmamer
summary report
save output
doe - 
clang vs g++
order of encoding
order of encoding in terms of operations
test fitness :when to do two opt {every gen, every 5, every 100, every 500, last 100}
population {replace in place, or get new array}
how often to mix population {every, every 5, every 100, every 1000, last 1000}
mutation {inverse vs scrambled}
crossover {tournament, roulette}
pairing {random, minimum, mix of both  }
euclidean vs squared
structs vs union vs objects
crossover resolution
sorting ny ponter or whole objects
estimate sorting